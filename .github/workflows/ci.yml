name: Monorepo CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main, release]

jobs:
  pre-flight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install
        run: npm ci
      - name: Check Formatting (Prettier)
        run: npx prettier --check .
      - name: Generate Prisma
        run: npm run generate -w api_tubes
      - name: Lint & Type Check All
        run: |
          npm run lint:all
          npm run types:all
      - name: Knip Cleanliness Check
        run: npx knip --reporter github-actions

  # --- Сборка всех пакетов параллельно ---
  build:
    needs: pre-flight
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Используем матрицу, чтобы не дублировать код джоб
        package: [api_tubes, tubes_application, new-app]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install
        run: npm ci
      - name: Generate Prisma (only for API)
        if: matrix.package == 'api_tubes'
        run: npm run generate -w api_tubes
      - name: Build ${{ matrix.package }}
        run: npm run build -w ${{ matrix.package }}
